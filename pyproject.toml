[project]
name = "redis-stream-claim-cursor-fence"
version = "0.0.1"
description = "Scaffold: Stream Claim Cursor Fence (independent Redis lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
